/**
 * lib/knowledge/hook-library.ts
 *
 * Hook pattern library — recurring opening formulas extracted from high-performing videos.
 * Populated as the pipeline processes more videos and identifies reusable hook archetypes.
 *
 * Future shape:
 *   { id, template, niche[], avg_engagement_lift, example_scripts[] }
 */

export const HOOK_LIBRARY: unknown[] = []
